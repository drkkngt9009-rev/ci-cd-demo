version: 0.2
phases:
  install:
    commands:
      - echo "Nothing to install in this simple example"
  build:
    commands:
      - echo "Building..."
  post_build:
    commands:
      - echo "Build completed on `date`"
artifacts:
  files:
    - '**/*'